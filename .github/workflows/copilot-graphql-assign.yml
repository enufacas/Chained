name: Copilot Assignment Workflow

on:
  issues:
    types: [opened]
  schedule:
    # Run every 3 hours to check for unassigned issues
    - cron: '0 */3 * * *'
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to assign to Copilot (leave empty to process all open issues)'
        required: false
        type: string

permissions:
  issues: write
  contents: read

jobs:
  assign-to-copilot:
    runs-on: ubuntu-latest
    # Only run if issue was just created, on schedule, or via workflow_dispatch
    if: |
      github.event_name == 'issues' ||
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch'
    steps:
      - name: Check for PAT configuration
        env:
          GH_TOKEN: ${{ secrets.COPILOT_PAT }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "⚠️ WARNING: COPILOT_PAT secret is not configured!"
            echo ""
            echo "The default GITHUB_TOKEN cannot assign issues to Copilot due to licensing restrictions."
            echo "You must create a Personal Access Token (PAT) with 'repo' scope and add it as a secret."
            echo ""
            echo "See COPILOT_SETUP.md for detailed setup instructions."
            echo ""
            echo "📚 Documentation: https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-a-pr"
            echo ""
            echo "Proceeding with GITHUB_TOKEN (will likely fail to find Copilot actor)..."
          else
            echo "✅ COPILOT_PAT is configured"
          fi
      
      - name: Discover and process issues
        env:
          # IMPORTANT: Use a Personal Access Token (PAT) instead of GITHUB_TOKEN
          # The default GITHUB_TOKEN does not have permissions to assign Copilot
          # See: https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-a-pr
          # Create a PAT with 'repo' scope and add it as a secret named COPILOT_PAT
          GH_TOKEN: ${{ secrets.COPILOT_PAT || secrets.GITHUB_TOKEN }}
        run: |
          echo "🔍 Determining which issues to process..."
          
          # If workflow_dispatch with specific issue number, process only that issue
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.issue_number }}" ]; then
            echo "Processing specific issue #${{ inputs.issue_number }}"
            issue_numbers="${{ inputs.issue_number }}"
          # If triggered by issue opened event, process only that issue
          elif [ "${{ github.event_name }}" = "issues" ]; then
            issue_numbers="${{ github.event.issue.number }}"
            echo "Processing newly opened issue #$issue_numbers"
          else
            # For scheduled runs or manual dispatch without issue number, get all open issues
            echo "Processing all open issues (scheduled or manual run)"
            issue_numbers=$(gh issue list --repo ${{ github.repository }} --state open --limit 1000 --json number --jq '.[].number')
          fi
          
          if [ -z "$issue_numbers" ]; then
            echo "ℹ️ No open issues found"
            exit 0
          fi
          
          echo "Found issues to check: $issue_numbers"
          
          success_count=0
          already_assigned_count=0
          failed_count=0
          
          # Process each issue
          for issue_number in $issue_numbers; do
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "Processing issue #$issue_number"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            # Get current assignees
            assignees=$(gh issue view $issue_number --repo ${{ github.repository }} --json assignees --jq '.assignees[].login')
            
            # Check if any assignee matches Copilot patterns
            if echo "$assignees" | grep -qE 'copilot|github-copilot'; then
              echo "✓ Issue #$issue_number already assigned to Copilot"
              already_assigned_count=$((already_assigned_count + 1))
              continue
            fi
            
            echo "📝 Issue #$issue_number not yet assigned to Copilot"
            
            # Check if we've already commented on this issue to avoid duplicate comments
            existing_comments=$(gh issue view $issue_number --repo ${{ github.repository }} --json comments --jq '.comments[].body')
            if echo "$existing_comments" | grep -q "🤖 **Copilot"; then
              echo "✓ Issue #$issue_number already has assignment comment, skipping to avoid duplicates"
              already_assigned_count=$((already_assigned_count + 1))
              continue
            fi
            
            # Add copilot-assigned label if not present
            labels=$(gh issue view $issue_number --repo ${{ github.repository }} --json labels --jq '.labels[].name')
            if ! echo "$labels" | grep -q "copilot-assigned"; then
              gh issue edit $issue_number --repo ${{ github.repository }} --add-label "copilot-assigned"
              echo "✓ Added copilot-assigned label to issue #$issue_number"
            fi
            
            # Analyze issue and determine appropriate agent specialization
            echo "🔍 Analyzing issue to determine appropriate agent specialization..."
            issue_data=$(gh issue view $issue_number --repo ${{ github.repository }} --json title,body --jq '{title: .title, body: .body}')
            issue_title=$(echo "$issue_data" | jq -r '.title')
            issue_body=$(echo "$issue_data" | jq -r '.body // ""')
            
            # Get list of available agent specializations
            available_agents=$(ls -1 .github/agents/*.md 2>/dev/null | xargs -n 1 basename | sed 's/\.md$//' | grep -v "README" || echo "")
            
            # Simple keyword matching to suggest appropriate agent
            suggested_agent=""
            agent_context=""
            if [ -n "$available_agents" ]; then
              # Check for bug-related keywords
              if echo "$issue_title $issue_body" | grep -qiE "bug|error|crash|fix|issue|problem|exception|fail"; then
                if echo "$available_agents" | grep -q "bug-hunter"; then
                  suggested_agent="bug-hunter"
                fi
              fi
              
              # Check for security keywords  
              if echo "$issue_title $issue_body" | grep -qiE "security|vulnerability|auth|injection|xss|csrf"; then
                if echo "$available_agents" | grep -q "security-guardian"; then
                  suggested_agent="security-guardian"
                fi
              fi
              
              # Check for feature/build keywords
              if echo "$issue_title $issue_body" | grep -qiE "feature|add|implement|create|build|new"; then
                if echo "$available_agents" | grep -q "feature-architect"; then
                  suggested_agent="feature-architect"
                fi
              fi
              
              # Check for documentation keywords
              if echo "$issue_title $issue_body" | grep -qiE "doc|documentation|readme|guide|tutorial"; then
                if echo "$available_agents" | grep -q "doc-master"; then
                  suggested_agent="doc-master"
                fi
              fi
              
              # Check for refactoring keywords
              if echo "$issue_title $issue_body" | grep -qiE "refactor|cleanup|improve|optimize|restructure"; then
                if echo "$available_agents" | grep -q "refactor-wizard"; then
                  suggested_agent="refactor-wizard"
                elif echo "$available_agents" | grep -q "performance-optimizer"; then
                  suggested_agent="performance-optimizer"
                fi
              fi
              
              # Check for testing keywords
              if echo "$issue_title $issue_body" | grep -qiE "test|testing|coverage|qa|validation"; then
                if echo "$available_agents" | grep -q "test-champion"; then
                  suggested_agent="test-champion"
                fi
              fi
              
              # If we found a matching agent, get its description
              if [ -n "$suggested_agent" ] && [ -f ".github/agents/${suggested_agent}.md" ]; then
                echo "✅ Suggested agent specialization: $suggested_agent"
                agent_context="

**🎯 Recommended Agent Specialization:** [\`$suggested_agent\`](.github/agents/${suggested_agent}.md)

This issue appears to be best suited for the **$suggested_agent** agent. When implementing, please follow the specialized approach and responsibilities defined in the agent definition."
              else
                echo "ℹ️ No specific agent specialization matched, will use general approach"
              fi
            fi
            
            # Get the issue node ID (required for GraphQL mutation)
            echo "🔍 Fetching issue node ID for issue #$issue_number..."
            issue_node_id=$(gh issue view $issue_number --repo ${{ github.repository }} --json id --jq '.id')
            
            if [ -z "$issue_node_id" ]; then
              echo "❌ Failed to get issue node ID for issue #$issue_number"
              failed_count=$((failed_count + 1))
              continue
            fi
            
            echo "📍 Issue node ID: $issue_node_id"
            
            # Get Copilot's actor ID from the repository's suggested actors
            echo "🔍 Fetching Copilot actor ID..."
            copilot_actor_id=$(gh api graphql -f query='
              query($owner: String!, $repo: String!) {
                repository(owner: $owner, name: $repo) {
                  suggestedActors(capabilities: [CAN_BE_ASSIGNED], first: 100) {
                    nodes {
                      login
                      __typename
                      ... on Bot { id }
                      ... on User { id }
                    }
                  }
                }
              }' -f owner="${{ github.repository_owner }}" -f repo="${{ github.event.repository.name }}" | jq -r '.data.repository.suggestedActors.nodes[] | select(.login | test("copilot"; "i")) | .id' | head -1)
            
            if [ -z "$copilot_actor_id" ]; then
              echo "❌ Could not find Copilot actor ID in suggested actors"
              echo "ℹ️  Copilot may not be enabled for this repository"
              failed_count=$((failed_count + 1))
              
              gh issue comment $issue_number --repo ${{ github.repository }} --body "⚠️ **Copilot Not Available**
          
          Could not find GitHub Copilot in the list of available assignees for this repository.
          
          **This means:**
          - GitHub Copilot is not enabled for this repository, OR
          - The workflow is not using a Personal Access Token (PAT) with proper permissions
          
          **To fix this:**
          1. **Enable Copilot:** Go to repository Settings → Copilot and enable GitHub Copilot
          2. **Set up PAT:** Create a Personal Access Token with \`repo\` scope at https://github.com/settings/tokens
          3. **Add Secret:** Add the PAT as a repository secret named \`COPILOT_PAT\`
          4. **Verify Subscription:** Ensure your account has a Copilot subscription (Individual, Business, or Enterprise)
          
          **Note:** The default \`GITHUB_TOKEN\` cannot assign Copilot due to licensing restrictions. A PAT is required.
          
          **For now:**
          - This issue has been labeled \`copilot-assigned\` for tracking
          - You can manually assign Copilot from the GitHub UI, or assign a developer
          
          ---
          *🤖 Automated check via GitHub GraphQL API - See https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-a-pr*"
              continue
            fi
            
            echo "📍 Copilot actor ID: $copilot_actor_id"
            
            # Assign issue to Copilot using GraphQL API
            echo "🤖 Assigning issue #$issue_number to Copilot via GraphQL API..."
            
            # Use GraphQL mutation to assign Copilot (this is the official method per GitHub docs)
            mutation_result=$(gh api graphql -f query='
              mutation($issueId: ID!, $actorId: ID!) {
                replaceActorsForAssignable(input: {
                  assignableId: $issueId,
                  actorIds: [$actorId]
                }) {
                  assignable {
                    ... on Issue {
                      id
                      assignees(first: 10) {
                        nodes {
                          login
                        }
                      }
                    }
                  }
                }
              }' -f issueId="$issue_node_id" -f actorId="$copilot_actor_id" 2>&1)
            
            if echo "$mutation_result" | jq -e '.data.replaceActorsForAssignable.assignable' > /dev/null 2>&1; then
              echo "✅ Successfully assigned issue #$issue_number to Copilot"
              success_count=$((success_count + 1))
              
              # Add success comment with agent context if available
              gh issue comment $issue_number --repo ${{ github.repository }} --body "🤖 **Copilot Assigned Successfully**

GitHub Copilot has been automatically assigned to this issue via the official GitHub GraphQL API.${agent_context}

**What happens next:**
1. ✅ Copilot will analyze the issue requirements
2. 💻 Copilot will create a branch and implement the solution
3. 📝 Copilot will open a PR with the implementation
4. 🔍 Auto-review workflow will validate and merge
5. ✅ Issue will be automatically closed when complete

**Estimated time:** Copilot typically starts work within a few minutes
**Assigned at:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')

---
*🤖 Automated via the official GitHub GraphQL API method using \`replaceActorsForAssignable\` mutation - True autonomous operation!*"
            else
              echo "❌ Failed to assign issue #$issue_number to Copilot"
              echo "Error details: $mutation_result"
              failed_count=$((failed_count + 1))
              
              # Add error comment with details
              gh issue comment $issue_number --repo ${{ github.repository }} --body "⚠️ **Failed to Assign Copilot via GraphQL API**
          
          The workflow attempted to assign GitHub Copilot to this issue using the official GraphQL API method, but the assignment failed.
          
          **API Method Used:**
          - GraphQL mutation: \`replaceActorsForAssignable\`
          - Actor ID: \`$copilot_actor_id\`
          
          **Most Common Cause:**
          This workflow is using the default \`GITHUB_TOKEN\` which cannot assign Copilot due to licensing restrictions. A Personal Access Token (PAT) is required.
          
          **To Fix:**
          1. Create a PAT with \`repo\` scope at https://github.com/settings/tokens
          2. Add it as a repository secret named \`COPILOT_PAT\`
          3. Re-run this workflow
          
          **Other Possible Reasons:**
          - GitHub Copilot is not enabled for this repository
          - Copilot coding agent is not activated for your organization
          - You need a GitHub Copilot subscription (Individual, Business, or Enterprise)
          - The repository lacks necessary Copilot permissions
          
          **For now:**
          - This issue has been labeled \`copilot-assigned\` for tracking
          - You can manually assign Copilot from the GitHub UI
          - Or assign a developer to implement this manually
          
          **To manually assign Copilot:**
          - Click on \"Assignees\" in the right sidebar
          - Select \"Copilot\" from the dropdown
          - Copilot should automatically pick up the issue
          
          ---
          *🤖 Automated via the official GitHub GraphQL API as documented at https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-a-pr*"
            fi
          done
          
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 Summary"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Successfully assigned: $success_count"
          echo "✓ Already assigned: $already_assigned_count"
          echo "❌ Failed: $failed_count"
          echo "Timestamp: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
