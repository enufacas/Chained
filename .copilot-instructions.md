# Repository Organization Guidelines

> **Created by @organize-guru** following clean code principles

## Purpose
This file defines rules for maintaining a clean and organized repository structure. Following these guidelines prevents clutter and ensures maintainability.

## Root Directory Policy

### ✅ Files That SHOULD Be in Root

**Core Documentation Only:**
- `README.md` - Main project overview
- `LICENSE` - Project license
- `FAQ.md` - Frequently asked questions
- `GETTING_STARTED.md` - Initial setup guide
- `QUICKSTART.md` - Quick reference guide
- `CONFIGURATION.md` - Configuration reference
- `AGENT_QUICKSTART.md` - Agent system quick start
- `AUTONOMOUS_SYSTEM_ARCHITECTURE.md` - Core architecture document
- `AUTONOMOUS_LOOP_IMPLEMENTATION.md` - Implementation guide
- `SYSTEM_RECREATION_GUIDE.md` - System recreation documentation
- `SYSTEM_RECREATION_PROMPT.md` - Recreation prompt

**Build/Config Files:**
- `.gitignore`
- `.github/` directory (workflows, actions, etc.)
- `requirements.txt`
- Configuration files specific to build tools

### ❌ Files That Should NOT Be in Root

**Implementation Summaries** → `docs/implementation-summaries/`
- Agent-related: `docs/implementation-summaries/agents/`
- Workflow-related: `docs/implementation-summaries/workflows/`
- Feature-related: `docs/implementation-summaries/features/`
- Infrastructure: `docs/implementation-summaries/infrastructure/`

**Investigation Reports** → `docs/investigations/`
- Analysis reports
- Bug investigation reports
- Health check reports
- Performance analysis

**Guides and Tutorials** → `docs/guides/`
- How-to guides
- Usage guides
- Validation guides
- Quickstart guides (except core ones)

**Architecture Documents** → `docs/architecture/`
- System design documents
- Architecture diagrams
- Technical specifications
- Component designs

**Test Files** → `tests/`
- All `test_*.py` files
- Test scripts
- Test data fixtures

**Utility Scripts** → `tools/` or `scripts/`
- Analysis scripts
- Data processing scripts
- Validation scripts
- One-off utilities

**Migration Scripts** → `scripts/migrations/`
- Database migrations
- Data migrations
- One-time migration utilities

## Directory Structure

```
Chained/
├── README.md                           # ✅ Core docs in root
├── LICENSE
├── FAQ.md
├── GETTING_STARTED.md
├── QUICKSTART.md
├── CONFIGURATION.md
├── AGENT_QUICKSTART.md
├── AUTONOMOUS_SYSTEM_ARCHITECTURE.md
├── AUTONOMOUS_LOOP_IMPLEMENTATION.md
├── SYSTEM_RECREATION_GUIDE.md
├── SYSTEM_RECREATION_PROMPT.md
├── .github/                            # GitHub configuration
│   ├── workflows/                      # GitHub Actions
│   ├── agents/                         # Agent definitions
│   └── instructions/                   # Path-specific instructions
├── docs/                               # All documentation
│   ├── implementation-summaries/
│   │   ├── agents/                     # Agent implementation docs
│   │   ├── workflows/                  # Workflow implementation docs
│   │   ├── features/                   # Feature implementation docs
│   │   └── infrastructure/             # Infrastructure docs
│   ├── investigations/                 # Investigation reports
│   ├── guides/                         # User guides
│   ├── architecture/                   # Architecture docs
│   └── tutorials/                      # Step-by-step tutorials
├── tests/                              # All test files
│   └── test_*.py
├── tools/                              # Utility scripts
├── scripts/                            # Operational scripts
│   └── migrations/                     # Migration scripts
├── learnings/                          # AI learning data
├── summaries/                          # Generated summaries
├── analysis/                           # Analysis outputs
├── examples/                           # Example code
├── schemas/                            # Data schemas
└── world/                              # World model data
```

## Naming Conventions

### File Naming
- Use `SCREAMING_SNAKE_CASE` for high-level documentation: `GETTING_STARTED.md`
- Use `kebab-case` for implementation details: `feature-implementation.md`
- Use descriptive names that indicate content: `agent-evaluation-summary.md` not `summary.md`

### Directory Naming
- Use `kebab-case` for directory names: `implementation-summaries/`
- Plural for collections: `guides/`, `investigations/`, `tests/`
- Clear hierarchy: `docs/implementation-summaries/agents/`

## When Creating New Files

### Decision Tree

1. **Is this core project documentation?**
   - YES → Root directory
   - NO → Continue to step 2

2. **Is this an implementation summary or completion report?**
   - YES → `docs/implementation-summaries/[category]/`
   - NO → Continue to step 3

3. **Is this an investigation or analysis report?**
   - YES → `docs/investigations/`
   - NO → Continue to step 4

4. **Is this a user guide or tutorial?**
   - YES → `docs/guides/` or `docs/tutorials/`
   - NO → Continue to step 5

5. **Is this architecture documentation?**
   - YES → `docs/architecture/`
   - NO → Continue to step 6

6. **Is this a test file?**
   - YES → `tests/`
   - NO → Continue to step 7

7. **Is this a utility script?**
   - YES → `tools/` or `scripts/`
   - NO → Consider if file is needed

## Enforcement

### For All Contributors
- Before creating files in root, check this guide
- PR reviewers should verify proper file placement
- Automated checks may reject PRs with improper file placement

### For Autonomous Agents
- **@organize-guru** periodically reviews and organizes
- Agents should follow this guide when creating documentation
- Issue #[number] tracks root directory cleanup

## Rationale (SOLID Principles)

Following **@organize-guru**'s clean code philosophy:

1. **Single Responsibility**: Each directory has one clear purpose
2. **Open/Closed**: Easy to add new docs without restructuring
3. **Liskov Substitution**: Files in same category are interchangeable
4. **Interface Segregation**: Directory structure segregates concerns
5. **Dependency Inversion**: High-level docs in root, details in subdirs

## Maintenance

- **Review Frequency**: Monthly or when root has >15 files
- **Owner**: @organize-guru
- **Last Cleanup**: 2025-11-19
- **Next Review**: 2025-12-19

## Questions?

See [FAQ.md](FAQ.md) or create an issue with the `documentation` label.

---

*Maintained by @organize-guru - Because clean code extends to clean repositories.*

---

## Gemini Consultation Escalation Pattern

The Chained repository supports human-controlled escalation to Google's Gemini 3 Pro Preview during Copilot sessions.

### When to Use "Ask Gemini"

Use the escalation pattern when you need:
- **Code fixes** for bugs or implementation issues (primary use case)
- **Actual implementations** when stuck on how to code a solution
- **Specific code locations** that need changes with before/after examples
- **Security fixes** with concrete code implementations
- **Performance improvements** with actual optimized code
- **Refactoring guidance** with working code transformations
- **Second opinions** on architectural decisions (with code examples)

### How to Use

**Pattern 1: Code Fix Request (Primary Use)**
```
"ask gemini to fix the authentication bug in tools/auth.py line 45"
```

**Pattern 2: Natural Language (General)**
```
"ask gemini about whether we should use REST or GraphQL for the agent API"
```

**Pattern 3: Explicit Agent Mention**
```
"@gemini-consultant what are the security implications of this approach?"
```

**Pattern 4: Command-Line Tool**
```bash
# General consultation
python3 tools/ask_gemini.py "How should I structure this API?"

# Code-fixing mode (NEW)
python3 tools/ask_gemini.py --fix-code "Auth allows expired tokens" \
  --file tools/auth.py --code "def validate_token(token): return jwt.decode(token, KEY)"
```

### What Happens

1. Copilot invokes `@gemini-consultant` agent
2. Agent uses `tools/ask_gemini.py` to consult Gemini 3 Pro Preview
3. **For code issues:** Gemini provides actual code fixes with before/after examples
4. **For architecture:** Gemini provides analysis with concrete implementation examples
5. Agent synthesizes response with repository context and specific file locations
6. You receive actionable solutions with implementation steps

### Prerequisites

Requires authentication (choose one):

**Option A: Google AI Studio (Recommended)**
```bash
export GEMINI_API_KEY="your-api-key"
```
Get key from: https://aistudio.google.com/app/apikey

**Option B: Vertex AI (For GCP Users)**
```bash
export GOOGLE_API_KEY="your-vertex-key"
export USE_VERTEX_AI=true
export GOOGLE_CLOUD_PROJECT="your-project-id"
```

### Documentation

- **Full Guide:** `docs/guides/ASK_GEMINI.md`
- **Agent Definition:** `.github/agents/gemini-consultant.md`
- **Tool Source:** `tools/ask_gemini.py`

### Protected Agent

`@gemini-consultant` is a protected agent (like `@troubleshoot-expert`) and cannot be deleted or voted off. This ensures the escalation capability remains available to all users.

---
## Copilot agent directives (for automated agents)

When a Copilot coding agent runs against this repository, follow these constraints to avoid excessive context uploads and to respect sensitive data:

- Only analyze files changed in the current commit or pull request. Do not attempt to read the entire repository by default.
- Honor .copilotignore at the repository root. Do not read or send files matched by this file.
- Skip binary files (images, archives, compiled artifacts). Only consider text source files.
- Limit per-file context to the first 300 lines or the first 50 KB; if deeper review is required, request the file explicitly.
- Do not include .git or internal git history in API payloads. Use git diffs to compute minimal patches.
- If the workspace base ref is missing, fetch full refs rather than falling back to sending entire repo contents.
- Never include environment/secret files (e.g., .env, *.pem, *.key) in requests.
- If the assembled payload would exceed ~1 MB, instead send a summary (filenames + relevant small diffs) or iterate per-file.

These directives exist to prevent large payload (413) errors and protect secrets.
---
